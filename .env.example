# 微信问卷系统 - 环境变量示例文件
# 复制此文件并重命名为.env，根据实际情况修改配置

# =================================================================================
# 核心配置
# =================================================================================

# 调试模式 - 开发环境设为True，生产环境必须设为False
# 注意：设置为False时，将自动应用生产环境安全配置
DEBUG=True  # 开发环境: True, 生产环境: False

# 允许访问的主机列表 - 开发环境可用*，生产环境必须指定具体域名
ALLOWED_HOSTS=*  # 开发环境: *, 生产环境: your-domain.com,www.your-domain.com

# 密钥 - 生产环境必须使用强随机字符串
# 生成方法: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-wa478o&xc3^6#_xo2c-h)sjyg%-0qtm#n(36jvsu+q=+)xr1b8

# =================================================================================
# 数据库配置（二选一）
# =================================================================================

# 方式1: 使用DATABASE_URL（推荐）
# 格式：
#   SQLite: sqlite:///db.sqlite3
#   MySQL: mysql://user:password@host:port/dbname
#   PostgreSQL: postgres://user:password@host:port/dbname
DATABASE_URL=sqlite:///db.sqlite3  # 开发环境使用SQLite
# DATABASE_URL=mysql://wechat_survey_user:your-strong-db-password@localhost:3306/wechat_survey  # 生产环境使用MySQL

# 方式2: 详细的MySQL配置（当不使用DATABASE_URL时使用）
# DB_NAME=wechat_survey
# DB_USER=wechat_survey_user
# DB_PASSWORD=your-strong-db-password
# DB_HOST=localhost
# DB_PORT=3306

# =================================================================================
# 微信配置
# =================================================================================

# 微信公众号AppID
WECHAT_APP_ID=your-wechat-app-id

# 微信公众号AppSecret
WECHAT_APP_SECRET=your-wechat-app-secret

# =================================================================================
# SSL配置
# =================================================================================

# 是否启用SSL/HTTPS - 开发环境设为False，生产环境配置SSL后设为True
USE_SSL=False  # 开发环境: False, 生产环境: True

# =================================================================================
# 额外配置（可选）
# =================================================================================

# 日志级别 - 开发环境可设为DEBUG，生产环境设为INFO或ERROR
# LOG_LEVEL=DEBUG
